import {Image} from "../../image";

export type Superior ={
    position:string;
    fullName:string;
    achievements?:string[]
    image:Image;
    militaryRank: string,
    phoneNumber?: string,
}

