import {Image} from "../../image";

export type Superior ={
    textContent?: string,
    position:string,
    fullName:string,
    achievements?:string[],
    image:Image,
    militaryRank: string,
    phoneNumber?: string,
    imgLeft: boolean
}

